# Frontend environment variables (Vite)
# Google OAuth 2.0 Client ID (Web application)
VITE_GOOGLE_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com

# Base URL of your backend API for production (include scheme)
# Example for Render/Fly/VM: https://your-backend.example.com
# For local dev, the Vite proxy handles /api so you can omit this.
VITE_API_BASE_URL=https://your-backend.example.com

# Optional: your deployed frontend origin if you want to suppress the in-app origin warning
VITE_FRONTEND_URL=https://cha-cha-chaudari.vercel.app

# Optional: dedicate a separate TTS server base URL (e.g., standalone Piper service)
# If set, the chat UI will call `${VITE_TTS_BASE_URL}/tts` for audio instead of the main API.
# Example (local Piper microservice): http://localhost:5001
VITE_TTS_BASE_URL=
